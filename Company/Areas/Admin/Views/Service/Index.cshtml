@model List<Service>


<div class="card shadow border-0 mt-4">
    <div class="card-header bg-info bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">Service List</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">

        <div class="row pb-3">
            
            <div class="col-12 text-end">
                <a asp-controller="Service" asp-action="Upsert" class="btn btn-info">
                    <i class="bi bi-plus-circle"></i>  Create New Service
                </a>
            </div>
        </div>

        <table id="tblData" class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Service Name
                    </th>
                    <th>
                        Description
                    </th>
                    <th></th>

                </tr>
            </thead>

@*             <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <th>
                            @item.Name
                        </th>
                        <th>
                            @item.Description
                        </th>
                        <th>
                            <div class="w-75 btn-group" role="group">
                                <a asp-controller="Service" asp-action="Edit" asp-route-id=@item.Id class="btn btn-info mx-2">
                                    <i class="bi bi-pencil-square"></i> Edit
                                </a>
                                <a asp-controller="Service" asp-action="Delete" asp-route-id=@item.Id class="btn btn-danger mx-2">
                                    <i class="bi bi-trash-fill"></i> Delete
                                </a>
                            </div>
                        </th>

                    </tr>

                }
            </tbody> *@
        </table>

    </div>
</div>
@section Scripts {
    <script src="~/js/service.js"></script>
}